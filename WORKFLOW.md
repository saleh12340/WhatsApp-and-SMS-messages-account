# ملف سير العمل: منظومة الحوالات والحسابات (علاقة حب وتناغم مالي وتقني)
### Workflow Architecture, Accounting Relations & Operational Lifecycle

---

## 1. الفلسفة العامة (علاقة حب وثقة بين المستخدم والنظام المحاسبي)
يقوم هذا النظام على فلسفة **"علاقة الحب والتناغم المالي"**؛ حيث تنتهي فوضى الرسائل المبعثرة وتوتر تتبع الحوالات، لتحل محلها علاقة ثقة وطمأنينة متكاملة بين المستخدم وتطبيق المحاسبة الآلي. يضمن النظام راحة البال عبر أتمتة دقيقة ترصد كل ريال قادم أو خارج وتضعه في مكانه المحاسبي الصحيح في أجزاء من الثانية دون أي تكرار أو خطأ بشري.

---

## 2. مخطط تدفق سير العمل (End-to-End Workflow Flowchart)

```mermaid
graph TD
    A[وصول رسالة مالية جديدة] --> B{نوع المصدر}
    
    B -->|رسالة نصية SMS| C[SmsReceiver Broadcast]
    B -->|إشعار واتساب| D[WhatsAppNotificationListener]
    B -->|تسليم نقدي / مقاصة| E[إدخال يدوي من المستخدم]
    
    C --> F{فحص قائمة الأهداف المعتمدة Targets}
    D --> F
    
    F -->|المرسل غير معتمد| G[تجاهل الرسالة بأمان وعدم حفظها]
    F -->|المرسل معتمد ومطابق| H[محرك التحليل والاستخراج الذكي RemittanceParser]
    
    E --> I[نموذج الإدخال والتحقق الفوري ManualEntryDialog]
    
    H --> J[معالجة وتوحيد الأرقام والمبالغ المشرقية]
    J --> K[التصنيف المحاسبي: دائن لكم (+) أو مدين عليكم (-)]
    K --> L[استخراج: المستفيد، رقم الحوالة، العمولة، الرصيد المتبقي]
    
    L --> M{فحص تكرار رقم الحوالة Reference Check}
    M -->|رقم الحوالة مسجل مسبقاً| N[تجاهل لتفادي التكرار المالي]
    M -->|حركة جديدة غير مكررة| O[حفظ فوري في قاعدة البيانات المحلية Room Database]
    I --> O
    
    O --> P[تحديث شاشة كشف الحساب والإجماليات والرصيد الصافي فوراً]
    O --> Q[محرك المزامنة السحابية الهجين SyncEngine]
    
    Q --> R{توفر اتصال بالإنترنت؟}
    R -->|أوفلاين - لا يوجد إنترنت| S[إبقاء الحالة محلياً PENDING]
    R -->|أونلاين - متصل| T[مزامنة آمنة وتحديث الحالة إلى SYNCED]
```

---

## 3. مراحل سير العمل التفصيلية (Workflow Phases)

### المرحلة 1: استقبال الرسائل والاستماع الفوري (Ingestion Layer)
- **قناة الرسائل النصية (SMS Receiver)**:
  - استقبال عبر `SmsReceiver` المعتمد في نظام أندرويد.
  - قراءة رقم أو اسم مرسل الـ SMS (مثل `AmalbwadiEX` أو `AlKuraimi`).
- **قناة واتساب (WhatsApp Notification Listener)**:
  - استماع مباشر عبر `WhatsAppNotificationListener` لإشعارات تطبيقي واتساب الرسمي وواتساب الأعمال.
  - استخراج عنوان الإشعار (اسم الشات أو المحادثة) ونص الرسالة الكامل.
- **قناة الإدخال اليدوي (Manual Entry)**:
  - تسجيل الحركات النقدية التي تتم يداً بيد أو عبر المقاصة المباشرة مع تسجيل الملاحظات والطرف الآخر.

### المرحلة 2: بوابة الفرز والتحقق من الأهداف (Target Filtering Gate)
- التحقق من مطابقة المرسل أو الشات مع قائمة الأهداف المعتمدة في جدول `target_configs`.
- إذا كانت الجهة غير مفعلة أو غير مدرجة، يتم رفض الرسالة فوراً دون استهلاك موارد الجهاز وحفاظاً على خصوصية المحادثات الشخصية.

### المرحلة 3: التحليل والتصنيف المحاسبي الذكي (Regex Parser)
- **توحيد الأرقام**: تحويل الأرقام المشرقية (`٠١٢٣٤٥٦٧٨٩`) والفواصل إلى صيغ رقمية قياسية.
- **التصنيف المحاسبي الدقيق**:
  - **حركة دائنة (Credit - لكم)**: تم إيداع، تم إضافة، حسبنا لكم، قيد لكم، استلام، وارد. (تزيد رصيدك وصافي حسابك).
  - **حركة مدينة (Debit - عليكم)**: تم خصم، تم تحويل، حسبنا عليكم، قيد عليكم، سحب، دفع للمستفيد. (تخصم من رصيدك وتعتبر التزاماً أو صرفاً).
- **استخراج الحقول الأساسية**:
  - المبلغ الأساسي + العمولة = الإجمالي المحتسب.
  - رقم الحوالة / السند المرجعي.
  - اسم الطرف المحول أو المستفيد.
  - الرصيد المتبقي بعد العملية (إن ورد في نص الإشعار).
  - الاحتفاظ بالنص الأصلي للمطابقة والتدقيق لاحقاً.

### المرحلة 4: التخزين المحلي الآمن (Offline-First Room Persistence)
- تخزين فوري في جدول `transactions` باستخدام Room DB.
- منع الازدواجية والتكرار عبر فحص رقم السند المرجعي `referenceNumber`.
- احتساب الرصيد التراكمي وتحديث واجهة المستخدم فوراً عبر `Kotlin StateFlow`.

### المرحلة 5: المزامنة السحابية الذكية (Cloud Sync Engine)
- يعمل التطبيق أولاً وبشكل مستقل بدون إنترنت (**100% Offline-First**).
- عند توفر شبكة الإنترنت، ينطلق `SyncEngine` لرفع العمليات غير المتزامنة وتغيير حالتها من `PENDING` إلى `SYNCED` دون إبطاء الواجهة.

---

## 4. مصفوفة العلاقات المحاسبية (علاقة الحسابات - Love & Balance Relationship)

| نوع الحركة | التوصيف المحاسبي | التأثير على الرصيد الصافي | أمثلة الرسائل والنصوص |
| :--- | :--- | :--- | :--- |
| **دائن (Credit)** | لكم / إيداع بحسابكم | **زيادة (+) في رصيدك** | "تم إضافة 150000 ريال لحسابكم من المحول سالم" |
| **مدين (Debit)** | عليكم / خصم من حسابكم | **نقص (-) من رصيدك** | "تم خصم 65000 ريال للمستفيد مؤسسة النور" |
| **عمولة (Fee)** | أجور التحويل والخدمة | **إضافية على الحركة** | "عمولة: 1000 ريال" |
| **الرصيد الصافي (Net)** | مجموع الدائن - مجموع المدين | **المركز المالي الحالي** | إذا كان موجب: لكم / إذا كان سالب: عليكم |

---

## 5. دورة حياة الحالات (State Machine)
1. `NEW_MESSAGE` (رسالة واردة عبر SMS / WhatsApp)
2. `VERIFIED_TARGET` (تم التأكد أنها من جهة مستهدفة)
3. `PARSED_SUCCESS` (تم تحليل المبالغ والتصنيف المحاسبي بنجاح)
4. `DEDUPLICATED` (تم التأكد من عدم تسجيل نفس السند مسبقاً)
5. `STORED_OFFLINE` (مسجلة في قاعدة بيانات الهاتف - حالة PENDING)
6. `SYNCED_CLOUD` (تمت المزامنة السحابية - حالة SYNCED)

---
*تم إنشاء وتنسيق هذا الملف ليكون مرجعاً معمارياً وهندسياً شاملاً لسير عمل منظومة الحسابات والحوالات.*
